#include "../../../cgen-lib/print.h"

int* addNumberAndPointer(int* a, int b) {
  int* sum = a + b;
  return sum;
}

int main () {

  int x[3] = {1,2,3};
  int *p;
  p = x;
  printf("%d\n", addNumberAndPointer(p, 1));

  return 0;
}
